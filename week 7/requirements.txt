python-dotenv>=1.0.0

# Core (required)
pypdf>=4.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
requests>=2.31.0

# Optional — better semantic embeddings (downloads model weights on first use)
sentence-transformers>=2.2.2

# LLM backends — install whichever you want to use
anthropic>=0.40.0       # set ANTHROPIC_API_KEY
groq>=0.9.0             # set GROQ_API_KEY  (fast cloud inference)
openai>=1.0.0           # set OPENAI_API_KEY
# Ollama needs no package — just run `ollama serve` locally
